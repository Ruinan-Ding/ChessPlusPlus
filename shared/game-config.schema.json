{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ChessPlusPlus Game Configuration",
  "description": "Configuration schema for a hex-grid tactics game. Uses axial coordinates (q, r). Matches DEFAULT_CONFIG in server/game/engine/config_loader.py and client config.service.ts.",
  "type": "object",
  "required": [
    "version",
    "board",
    "units",
    "setup",
    "rules"
  ],
  "properties": {
    "version": {
      "type": "string",
      "description": "Schema version for forward compatibility."
    },
    "board": {
      "type": "object",
      "required": [
        "radius"
      ],
      "properties": {
        "radius": {
          "type": "integer",
          "minimum": 1,
          "maximum": 50,
          "description": "Hex grid radius (distance from centre hex to edge). The default board is radius 11 (12 cells per hexagon edge)."
        },
        "orientation": {
          "type": "string",
          "enum": [
            "edge-up",
            "vertex-up"
          ],
          "description": "Cosmetic: how the client draws the board hexagon. Default is edge-up."
        }
      },
      "additionalProperties": false
    },
    "units": {
      "type": "object",
      "description": "Unit type definitions, keyed by unit id.",
      "additionalProperties": {
        "type": "object",
        "required": [
          "id",
          "name",
          "symbol",
          "move",
          "value",
          "hp",
          "attack",
          "defense"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "symbol": {
            "type": "string",
            "maxLength": 4
          },
          "display": {
            "type": "object",
            "description": "Per-colour display glyphs rendered on the board.",
            "properties": {
              "white": {
                "type": "string"
              },
              "black": {
                "type": "string"
              }
            },
            "additionalProperties": false
          },
          "move": {
            "type": "integer",
            "minimum": 0,
            "description": "Number of adjacent-hex steps this unit can move per turn. Movement floods outward through the six hex neighbours. A unit walks through its own - an ally costs a step to pass but is not somewhere to stop - while an enemy blocks the hex and the way past it alike."
          },
          "value": {
            "type": "integer",
            "minimum": 0
          },
          "hp": {
            "type": "integer",
            "minimum": 1,
            "description": "Maximum hit points for this unit type."
          },
          "attack": {
            "type": "integer",
            "minimum": 0,
            "description": "Attack damage dealt when this unit initiates combat."
          },
          "commander": {
            "type": "boolean",
            "description": "This unit is the side's commander. Under the 'regicide' objective, losing every commander loses the game. Omitted means false."
          },
          "attackRange": {
            "type": "integer",
            "minimum": 1,
            "maximum": 50,
            "description": "How many rings of adjacency this unit can strike, by hex distance and ignoring obstacles. 1 = the six neighbouring hexes. Beyond the first ring damage falls off by rules.rangeFalloff per extra ring. Omitted means 1."
          },
          "defense": {
            "type": "integer",
            "minimum": 0,
            "description": "Flat damage reduction applied when this unit is attacked. strike_damage() subtracts it from the incoming attack, floored at 0."
          }
        },
        "additionalProperties": false
      }
    },
    "abilities": {
      "type": "object",
      "description": "The ability catalogue and how a side gets at it. Keyed by a STABLE id throughout - never by position - because a side's saved loadout, path and cooldowns are written by id, and a reordered list would otherwise re-point every one of them. The engine still does not read abilities; the client does, and this is where their numbers live so that tuning one is a config edit.",
      "properties": {
        "slots": {
          "type": "integer",
          "minimum": 0,
          "description": "How many pool abilities a side may carry at once."
        },
        "pool": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Catalogue ids a side picks its loadout from."
        },
        "paths": {
          "type": "array",
          "description": "The ways a side can specialise. One per match.",
          "items": {
            "type": "object",
            "required": [
              "id",
              "name",
              "cost",
              "passive",
              "skill",
              "ultimate"
            ],
            "properties": {
              "id": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "cost": {
                "type": "integer",
                "minimum": 0,
                "description": "CP to unlock the path."
              },
              "passive": {
                "type": "string",
                "description": "Catalogue id, granted free with the path."
              },
              "skill": {
                "type": "string",
                "description": "Catalogue id."
              },
              "ultimate": {
                "type": "string",
                "description": "Catalogue id, once a match."
              }
            },
            "additionalProperties": false
          }
        },
        "catalogue": {
          "type": "object",
          "description": "Every ability, keyed by its own id.",
          "additionalProperties": {
            "type": "object",
            "required": [
              "id",
              "name",
              "target"
            ],
            "properties": {
              "id": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "description": {
                "type": "string"
              },
              "target": {
                "enum": [
                  "friendly",
                  "enemy",
                  "universal"
                ],
                "description": "What a cast is aimed at; 'universal' needs no target."
              },
              "cost": {
                "type": "integer",
                "minimum": 0,
                "description": "Points for a pool ability, CP for a path's."
              },
              "cooldown": {
                "type": "integer",
                "minimum": 0
              },
              "mov": {
                "type": "integer"
              },
              "atk": {
                "type": "integer"
              },
              "def": {
                "type": "integer"
              },
              "damage": {
                "type": "integer",
                "minimum": 0
              },
              "heal": {
                "type": "integer",
                "minimum": 0
              },
              "points": {
                "type": "integer",
                "minimum": 0,
                "description": "Points the cast hands its caster."
              },
              "testing": {
                "type": "boolean",
                "description": "A testing lever, not a balanced ability. Kept out of networked play: Rally hands out 300 points, which would break the wrap's price."
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "setup": {
      "type": "object",
      "required": [
        "white",
        "black"
      ],
      "properties": {
        "white": {
          "type": "object",
          "description": "Map of 'q,r' coordinate strings to unit type ids for white.",
          "propertyNames": {
            "pattern": "^-?\\d+,-?\\d+$"
          },
          "additionalProperties": {
            "type": "string"
          }
        },
        "black": {
          "type": "object",
          "description": "Map of 'q,r' coordinate strings to unit type ids for black.",
          "propertyNames": {
            "pattern": "^-?\\d+,-?\\d+$"
          },
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "rules": {
      "type": "object",
      "required": [
        "maxTurns",
        "turnTimeLimit"
      ],
      "properties": {
        "maxTurns": {
          "type": "integer",
          "minimum": 0,
          "description": "0 = unlimited turns."
        },
        "turnTimeLimit": {
          "type": "integer",
          "minimum": 0,
          "description": "Per-turn time limit in seconds (0 = unlimited)."
        },
        "rangeFalloff": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Fraction of attack damage lost per ring beyond the first, applied linearly: ring 2 deals (1 - falloff) of the attack stat, ring 3 (1 - 2*falloff), floored, never below 1."
        },
        "minStrikeDamage": {
          "type": "integer",
          "minimum": 0,
          "description": "The least a blow that lands may deal, after defence is subtracted. 1 (the default) means armour blunts a hit but never turns it aside: without it a pawn (14 attack) took nothing at all off a shieldman (18 defence), so whole matchups were dead on the board with no rule saying so. 0 restores that, letting armour absorb a hit entirely. An attacker with no attack stat deals 0 either way - this lifts a blow that was blunted, not one that was never thrown."
        },
        "objective": {
          "type": "string",
          "enum": [
            "regicide",
            "elimination"
          ],
          "description": "Win condition. 'regicide' (the default): a side loses when it has no commander left. 'elimination': a side loses when it has no units left at all."
        },
        "panelMoversPerTurn": {
          "type": "integer",
          "minimum": 0,
          "description": "How many units each panel may start in one turn, counted for the base and the reserve separately. Default 3."
        },
        "phaseInitEntries": {
          "type": "integer",
          "minimum": 0,
          "description": "How many units a side may bring out of its reserve in a phase initialization. Stands instead of panelMoversPerTurn for the reserve on that turn. Default 5."
        },
        "homecomingsPerSetupTurn": {
          "type": "integer",
          "minimum": 0,
          "description": "How many units a side may walk home in one setup turn. Overtime has no count; the turn's board-move allowance caps it there. Default 3."
        },
        "cpPerPhase": {
          "type": "integer",
          "minimum": 0,
          "description": "CP handed to each side at the start of every phase, to buy abilities with. Default 100."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
